# For example, given [100, 4, 200, 1, 3, 2], the longest consecutive element sequence is [1, 2, 3, 4]. Return its length: 4.



def longseq(arr):
    arr.sort()
    print(arr)
    pivotpos = 0
    for cur in range(1, len(arr)):
        if arr[pivotpos] + 1 == arr[cur]:
            pivotpos = cur
            continue
        else:
            break
    print(len(arr[:cur]))


arr = [100, 4, 200, 1, 3, 2, 5]

longseq(arr)
